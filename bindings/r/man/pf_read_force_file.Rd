% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{pf_read_force_file}
\alias{pf_read_force_file}
\title{Read a force file}
\usage{
pf_read_force_file(
  path,
  sample_rate_hz = NULL,
  delimiter = NULL,
  force_column = NULL,
  sentinel_convention = "none",
  acquisition = NULL,
  plate = NULL,
  plates_folder = NULL
)
}
\arguments{
\item{path}{Path to a delimited text export.}

\item{sample_rate_hz}{The rate the trace was recorded at.}

\item{delimiter}{The single character separating this file's columns.}

\item{force_column}{Zero-based index of the column carrying vertical ground reaction
force.}

\item{sentinel_convention}{How this export writes a missing sample: `"none"`, `"zero"`
or `"negative_one"`.}

\item{acquisition}{What the plate and its settings were, from [pf_acquisition()].}

\item{plate}{A saved plate to fill the block from: a name this machine holds, or the list
[pf_plate()] returns. A member in `acquisition` beside it is the answer that runs, and
the result records what it replaced.}

\item{plates_folder}{Where to look for `plate`, when it is a name. Absent reads the folder
`plateforce plate save` writes to.}
}
\value{
A `trial` whose `@read_report` names the delimiter, the column, the rows read,
  the columns per row, the blank lines skipped, the samples that matched the sentinel
  convention, and the samples that carried no number at all.
}
\description{
The file is read by the engine. Nothing in this package parses it, so there is one
answer to which column carries force and one answer to what a row means.
}
\details{
Every choice the read rests on is stated by the caller and reported back. A rate that
is guessed scales every velocity, displacement, impulse and rate of force development
with it, and a column that is guessed can be the wrong one quietly, so neither has a
default.
}
